A simple Python program that generates a QR code from a user-provided link using the qrcode and Pillow libraries.
The generated QR code is saved as an image file (link_qr.png), making it easy to share URLs visually.
